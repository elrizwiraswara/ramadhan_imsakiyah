import 'package:flutter/material.dart';

/// Custom colors that may different from [AppTheme] colorSchemes
class AppColors {
  /// This class is not meant to be instatiated or extended; this constructor
  /// prevents instantiation and extension.
  AppColors._();

  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF323232);
  static const Color day = Color(0xFF2eb0ec);
  static const Color morning = Color(0xFF85cdd3);
  static const Color night = Color(0xFF35184f);
  static const Color afternoon = Color(0xFFf99561);
}
